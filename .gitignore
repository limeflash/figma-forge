node_modules/
*.log
.DS_Store
.env
.env.local

# Local design-system index cache and write journals live under
# CLAUDE_PLUGIN_DATA, not here — see mcp-server/src/store.ts.
.figma-forge/

# dist/ is intentionally NOT ignored. Claude Code installs a plugin by cloning
# this repo with no build step, so mcp-server/dist and figma-plugin/dist are
# shipped artifacts and must be committed.
