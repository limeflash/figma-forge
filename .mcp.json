{
  "_comment": "Development config for working ON Figma Forge in this repo. Claude Code loads a project-root .mcp.json with the project directory as cwd, so a relative path resolves. The config shipped to installed plugins is mcp-server/mcp.json, which uses ${CLAUDE_PLUGIN_ROOT} — that placeholder is empty here, which is why the two files cannot be one.",
  "mcpServers": {
    "figma-forge": {
      "command": "node",
      "args": ["mcp-server/dist/index.js"],
      "env": {
        "FIGMA_FORGE_BRIDGE_PORT": "3055"
      }
    }
  }
}
