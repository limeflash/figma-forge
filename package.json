{
  "name": "figma-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Design-system-aware Figma authoring for Claude Code.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit -p figma-plugin/tsconfig.json && tsc --noEmit -p mcp-server/tsconfig.json",
    "bridge": "node bridge/server.mjs",
    "gen:api": "node scripts/gen-api-reference.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  }
}
